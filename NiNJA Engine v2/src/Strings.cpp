#include "Strings.h"


// Global strings
SecureString *Strings::sOff; 
SecureString *Strings::sDefault;
SecureString *Strings::sNone;
SecureString *Strings::sSelected;
SecureString *Strings::sBest;
SecureString *Strings::sBoth;
SecureString *Strings::sNormal;

 // Aimbot strings
SecureString *Strings::sRage;
SecureString *Strings::sLegit;
SecureString *Strings::sSilent; 

// Tags
SecureString *Strings::sNeck;
SecureString *Strings::sHead;
SecureString *Strings::sAnkleLe;
SecureString *Strings::sAnkleRi;
SecureString *Strings::sWristLe;
SecureString *Strings::sWristRi;
SecureString *Strings::sMainroot;

// ESP Bones
SecureString *Strings::sFriendly;
SecureString *Strings::sEnemy;

